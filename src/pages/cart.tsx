import { Grid, Typography } from "@mui/material";
import React from "react";

export const Cart = () => {
  return (
    <Grid item container p={2}>
      <Typography sx={{ color: "black" }}>Cart Page</Typography>
    </Grid>
  );
};
