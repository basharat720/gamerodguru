import { Grid, Typography } from "@mui/material";
import React from "react";

export const Checkout = () => {
  return (
    <Grid item container p={2}>
      <Typography sx={{ color: "black" }}>Checkout Page</Typography>
    </Grid>
  );
};
