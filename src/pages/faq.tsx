import { Grid, Typography } from "@mui/material";
import React from "react";

export const FAQs = () => {
  return (
    <Grid item container p={2}>
      <Typography sx={{ color: "black" }}>FAQs page</Typography>
    </Grid>
  );
};
